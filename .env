# .env — flags shared by every llama-server instance regardless of backend.
# Backend-specific build/server flags live in rocm/.env and vulkan/.env.
SERVER_FLAGS_COMMON="--host 0.0.0.0 --models-max 1 --webui-mcp-proxy"
